<?php
    // user input data will come here for GET method


    echo $_GET['firstname']; // here we write "$_GET" because already we selected the method as GET in form. "firstname" is the value of name attribute.
    echo "<br>";
    echo $_GET['lastname'];  //"lastname" is the value of name attribute.


 
 
 
    // $_REQUEST[] also can use as alternet of $_GET[].

 

 
 ?>
 